version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./flask_session:/home/site/wwwroot/flask_session
    environment:
      - WEBSITES_PORT=8000
